<div class="col-12 {{ $class }}">
    <div class="divider divider-{{ $type }}">
        <div class="divider-text">
            @if ($heading)
                <h4 class="form-section mt-2 mb-2 text-center">{{ $text }}</h4>
            @endif
            {{ $text }}
        </div>
    </div>
</div>
